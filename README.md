# 评测集

`cases.json` 是面向作品集演示的模拟评测集，不代表线上业务指标。它覆盖：

- 标准故障码、重复告警和设备离线
- 模糊描述、未知故障码和无关问题
- 知识库有证据与无证据两类情况
- 高风险人工复核与工单草稿边界

运行：

```powershell
python evals/run_eval.py
```

脚本会输出引用命中率、人工复核判断匹配率、缺失证据识别准确率和工单字段完整性。当前评测使用 SQLite、规则和本地演示知识库，不调用外部模型，也不会上传数据。
